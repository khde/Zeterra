#ifndef CAMERA_H
#define CAMERA_H

struct Camera {
    unsigned int x;
    unsigned int y;
    // Implement struct Pos so I can pass pointer to it and follow it?
    // struct Pos *follow;
};

#endif
